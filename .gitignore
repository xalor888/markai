# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# 本地预览/临时文件与密钥
preview-*.png
.env
.env.*
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 内部会话/计划文件（不上传）
.zcode/
# keepgoal 的内部状态（台账、访问记录），不该进版本库
.keepgoal/
# 助手的项目记忆（工作日志/长期约定），属于本地状态，不该进版本库
.workbuddy/
# falsify 的中断自保备份（内含某源文件的完整原文，只在运行期间存在；
# 万一被强杀残留，也不该被误提交）
.falsify-pending.json
