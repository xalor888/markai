# DIRECTION — MarkAI 该往哪走

> 本文由 keepgoal 链在无人值守下自主撰写，每完成一个目标会回看并修订。
> 最后核实时间：2026-09-14（证据见「现状核实」）。

## 0. 一句话

MarkAI 的赌注是：**把书签库的最高管理权交给一个会自己动手的 Agent，用户仍然敢用。**
这个赌注成立与否，不取决于工具数量（已有 30 个），而取决于两件事：Agent 动手之后的**可逆性**，
以及我们对自己代码的**验证能力**是否配得上这种权限。

## 1. 项目是什么

Chrome/Edge MV3 浏览器扩展（WXT + React 19 + TS + Tailwind v4 + Zustand）。
侧边栏 / 完整页三栏双形态，聊天即操作：整理、扫描、查重、死链实测、批量清理。

不可替代点（相对"另一个书签管理器"）：

1. **自由对话 + 工具循环**，不是表单化的预设动作；工具轮数无上限（有死循环与连续失败兜底）。
2. **删除安全闸门**：Agent 只能提交删除提议，人在聊天卡片或待删清单里确认后才真正 `removeTree`；
   根文件夹永不可删；提议有终态保护，不会被后续工具调用复活。
3. **本地优先**：API Key 只存 `chrome.storage.local`，请求由后台直连所选服务商，不经任何中间服务器。

定位边界（**不**声称、**不**假装）：不是 Telegram/微信/QQ 的替代品，不碰同步服务端，
不做账号体系；它是"浏览器本地书签库的 Agent 操作台"。

## 2. 现状核实

本次（2026-09-14）实跑，非推测：

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 类型 | `npm run compile` | 通过（tsc 无输出，exit 0） |
| 测试 | `npm test` | **335 项全绿**（… → 307 → 314 → 325 → 335） |
| 构建 | `npm run build` | 通过，`.output/chrome-mv3` 788.17 kB |
| 版本 | `package.json` | **0.2.15（已发版）** |
| CI | `.github/workflows/ci.yml` | push/PR 跑 compile + test + build |
| 发布 | `.github/workflows/release.yml` | `v*` tag → 构建 + Release |

**P0 已落地**（见 §3）：15 个文件、+330/−146 行的未提交健壮性修复已补测并入 `main`，
其中一条被本轮**证伪并纠正**。内容质量抽查后确认方向正确，例如：

- `move_bookmark` 的 `fromPath` 在 move **之前**解析（原先移动后解析，返回的是新路径，字段名说谎）；
- `resolvePath` 不再把元根（id `0`）算进路径（原先每条路径都以 `(未命名)` 开头）；
- `check_urls` 的 skipped 逐条输出，保持 entry 形状一致；
- `merge_folders` / `open_bookmarks` 单条失败不再虚报 `moved++` / `opened.push`；
- `background.ts` 按消息类型回发匹配的响应形状（原先非删除类消息抛错也回 `deletions:result`）；
- 聊天面板拖拽 `chatWidthBase` 基准、删除对话框 `useMemo` 消除无限重渲染、
  跨窗口合并按"内容量"取更新的一方。

**P1 已落地**（见 §4A）：操作日志 + 一键撤销。新增 `src/lib/undo/`（事务模型 / 逆操作执行器 /
带记录的写操作包装），`tools.ts` 的所有写操作改走包装埋点，按 Agent 轮次聚合撤销点并落盘。
加上 P0，两次「回滚实现 → 测试必须变红」反证共 16 条全绿。

## 3. 关键缺陷（本轮实抓，附证据）

**我们的验证能力低于我们的声称。** 具体实例：

`tests/agent.test.ts` 的替身（P0 前）是：

```ts
move: async (id: string, dest: { parentId: string }) => { ... }   // 完全忽略 index
```

而同一文件里有一条测试叫
**「move_bookmarks 保持输入顺序（逆序 index 移动）」** —— 它断言的正是 `index` 语义，
但替身根本不实现 `index`。这条测试**在当时不可能失败**：书签 3、4 本来就排在 5 前面，
`childrenOf` 按插入顺序返回，于是 `kids[0]==='GitHub 主页'` 恒真。

更糟的是，未提交的那批修复里有两处**正是**同文件夹重排的下标逻辑
（`bookmark-list.tsx` 与 `bookmark-tree.tsx`），它们**完全没有测试**，
而唯一的"相关测试"是假绿的。

真实语义已从 Chromium 源码核实（`components/bookmarks/browser/bookmark_model.cc` `BookmarkModel::Move`，
Chrome 120 / 126 / 138 / trunk 四个版本一致；`chrome/browser/extensions/api/bookmarks/bookmarks_api.cc`
把 `index` 原样透传；`bookmark_model_unittest.cc` 的 `MoveToSameParent` 断言
「Move to current_index + 1 is a no-op」）：

```cpp
if (old_parent == new_parent && (index == old_index || index == old_index + 1)) {
  return;                        // 已在该位置，空操作
}
if (old_parent == new_parent && index > old_index) {
  index--;                       // 浏览器内部把「移除前下标」换算成「移除后下标」
}
```

**结论（本目标中实测得出，推翻了我们最初的判断）：`index` 是「移除源之前」坐标系里的插入位置，
浏览器自己会换算，调用方必须原样传递。那两处 `targetIndex - 1` 不是修复，而是一个 off-by-one 回归：
向后拖一格会得到 `index == oldIndex + 1`，正好命中上面的空操作——拖拽静默失效。**

这件事本身就是 §3 论点的最好例证：一个带着自信注释的改动、一条永远为真的测试、
一个没人验证过的 API 语义，三者叠加起来看起来完全正常。我们靠**读源码 + 会失败的测试**才拦住它。

因此：`index` 语义被固化在 `src/lib/bookmark-dnd.ts` 的 `resolveDropIndex`（一个"只返回落点"的函数，
存在的唯一理由就是钉住这条契约并让测试能证伪），替身也按真实语义实现。
这与"删除安全闸门"是同一件事的两面：**给 Agent 最高权限的前提，是我们能证明它在边界上做对了。**

**同类的第三个实例（发版时抓到）**：CI 的「上传构建产物」步骤连续三个版本（v0.2.0 / v0.2.1 / v0.2.2）
都是**绿的，却什么都没上传**。根因是 WXT 的产物在 `.output`（点开头的隐藏目录），
而 `actions/upload-artifact@v4.4+` 默认 `include-hidden-files: false`；
唯一的痕迹是 run 页面 ANNOTATIONS 里一句 `No files were found with the provided path`。
它和"假绿测试"是同一个病：**绿只说明"这一步没抛错"，不等于"这件事做成了"。**
所以发版后的验收对象是 run 的 ANNOTATIONS、Release 的资产列表、以及下载回来的
`manifest.json`——而不是 workflow 的结论。

## 4. 三条主线

### A. 可信（Trust）—— 让 Agent 的每一次动手都可逆

当前只有**删除**有闸门。其余变更（移动 / 重命名 / 改 URL / 新建 / 复制 / 合并 / 自动分类 /
排序 / 一键清理）都是**不可逆**的：Agent 一次误判，用户的书签库结构就被永久改写。
这是"最高管理权"最大的、也是唯一真正危险的空洞。

方向：**操作日志（operation journal）+ 一键撤销**。

- 每个写工具在动手前记录足够还原的状态：移动记 `(id, fromParentId, fromIndex)`，
  重命名/改 URL 记旧值，新建（含复制）记新建出的 id（撤销 = 删除），
  **删除记整棵子树快照 + 位置信息（撤销 = 递归重建）**。
- 撤销以**对话轮 / 任务**为粒度（"撤销刚才那一步"），而不是全局时间线——因为它服务于聊天交互。
- 撤销自身也要是透明的：说清"将还原 N 项"或"为什么不能撤"，且失败如实上报（含失败明细）。
- 与现有删除闸门正交：删除提议仍走确认流程，撤销覆盖其余写操作。

这条主线的验收物是**可执行的证据**，不是文档：测试替身里跑「Agent 批量移动 20 项 →
撤销 → 树结构与操作前逐节点相同（含顺序）」。

**已实现（P1）**：`src/lib/undo/`

- `types.ts` / `journal.ts`：纯逻辑（摘要、可撤销性判定、逆序计划），不碰 `chrome.*`，直接单测；
- `recorder.ts`：一个 Agent 轮次 = 一个撤销点，落盘 `chrome.storage.local`（`markai.undo`），保留最近 10 个；
- `mutations.ts`：替换 `tools.ts` 里全部 21 处裸写操作。顺序固定为「先取还原状态 → 再动手 → 再记日志」；
- `apply.ts`：逆序执行还原。**撤销自身走裸 API**，不记入日志（否则"撤销的撤销"会成死循环）。

**这一版学到的硬知识（血的教训，勿"简化"）**：同父目录还原时，`fromIndex` 不能直接回放。
Chromium 的 `index` 是「移除源之前」坐标，而我们要的是「移除后落到第 fromIndex 位」：
设节点当前在同父列表里的下标为 `c`、目标位为 `p`，则 `p < c` 时传 `p`，`p >= c` 时传 `p + 1`。
直接回放会在 `p >= c` 时命中 `index == oldIndex + 1` 的空操作，表现为**排序撤销后顺序错位**。
这个 bug 是端到端深比对测试（而非"按钮能点"）抓出来的——第一版实现就栽在这里。

另外两条刻意的设计取舍：
- **旧日志点（没有删除快照的）整轮拒绝撤销**，并说明原因——做半撤销会让用户以为已还原；
  新写入的删除都带快照，因此含删除的轮次现在**可以**完整撤销；
- **只读轮次不产生撤销点**，避免出现「撤销 0 项」的空按钮。

**删除可撤销（在 P2 之后补上，闭合最后一块不可逆）**：`cleanup_sweep` / `propose_deletions` /
`delete_all_bookmarks` 在「无需确认」模式下真正删书签，此前这类轮次整轮不可撤。
现在删除前记整棵子树快照，撤销时递归重建。

实现过程中被测试打回**两次**，两次都值得记住：

1. **位置锚点的推理是错的。** 第一版记录「删除前紧随其后的兄弟 id」，理由是「逆序还原时它一定
   已经回来了」。这在**串行**下成立，并发下不成立：**抓取顺序 ≠ 记录顺序**，锚点可能还没被还原
   就到它了。直接并发用例碰巧过了，**端到端跑 `cleanup_sweep` 才把它打出来**
   （`s-01` 落到 `s-10` 的位置）。正解是工具在动手**之前**为每个将失去子项的父目录拍一次
   「完整子序检查点」，撤销最后按检查点整体校正顺序——与并发无关。
2. **重建的节点是新 id。** 检查点里存的是删除前的旧 id，而 Chrome 重建后分配新 id，
   不建立 old→new 映射的话检查点根本对不上（表现为顺序还原静默无效）。这条也是测试抓的。

位置信息有两级：**检查点**（精确，工具负责在动手前取）+ 逐条 `index`（兜底，仅在缺少检查点时用，
并发下不可靠）。这条契约写在 `mutations.ts` 的 `jRemove` 注释里，调用方必须遵守。

**同一类失败的第四次清扫：设置保存（2026-09-14 修）**：`configStore.update()` 也是"先 set 让 UI 显示新值、
再静默 catch 写盘"。这条路径存的是 **API Key、Base URL、模型与删除模式**，于是有两个真实后果：
API Key 静默丢失（下次启动聊天莫名其妙失败）；更严重的是**安全相关**——把删除模式从"无需确认"切到
"需要确认"时若保存失败，界面显示已切换、存储里仍是"无需确认"，重载后 Agent 会**不打招呼就删**，
而用户以为自己受保护。现在：失败如实上报（带 `getBytesInUse` 真实占用）、`update()` **返回如实结果**
（`{ok,error}`，重试按钮据此给真实反馈，不再是成败同一句话）、设置页显示可见警示并可一键重试、
**删除模式的失败文案直接点名风险**。

同时把判定规则固化成 `docs/error-handling.md`：什么时候必须如实上报、什么时候可以 best-effort 忽略
（并列出各类"合理忽略"的理由），以及**仍未逐处审计**的部分（如实写明：`catch {` 64 处、`.catch(() => …)`
84 处，多数属合理忽略，但有几类高风险还需要逐个判定）。这份文档的价值在于让"哪些失败被吞了"变成
可核对的清单，而不是散落在代码注释里。

**对话存储的同类静默失败（2026-09-14 修）**：`aiStore._persist` 的写盘失败原先是一个**空 catch**
（注释称"下次写入会重试"，但超配额时之后每次都会失败），而界面照常显示历史——**把"没保存成功"呈现成"好好的"**。
这与撤销那条是同一类错误，只是长在另一条存储路径上；而且聊天更可能先撞配额：单会话消息有上限（60 条）
但**会话数量没有上限**。现在：

- 纯函数 `trimConversationsToBudget()` 做**确定且可解释**的裁剪：超预算时从最旧会话丢最旧消息，
  必要时整个丢弃最旧会话，始终保留最新的（`src/lib/ai/chat-budget.ts`，可直接单测）；
- 预算 **6 MiB**，与撤销的 4 MiB 并列，避免两类数据互相踩踏；
- 丢弃会**如实说明**（"为了保存最近的对话，已丢弃更早的 N 条消息 / M 个会话"），不静默降级；
- 写盘失败不再吞掉：记录带**稳定时间戳**的失败状态（同一次故障只提示一次，避免流式期间反复弹窗），
  提示文案写明"这段对话没有保存成功"并附上 `getBytesInUse()` 读到的**真实占用**；
- UI 上是一条**常驻警示条**（不是一闪而过的 toast），可手动关闭。

至此 `chrome.storage.local` 里的两坨主要数据都有如实护栏；`getBytesInUse()` 也用上了两处：失败时说清
**真实占用**，成功时再用它**核对我们的估算**（估算达标但浏览器说超标 → 同样如实告知，因为"写成功"不等于"占用合理"）。
替身补了这个 API（按 JSON 字节数建模），预算另有测试缝（`setChatBudgetBytes`）以便确定性地验证这条路径。

**被中断的轮次也能撤销（2026-09-14 修）**：原先 `endUndoTransaction` 是**唯一**落盘点——
操作只存在内存里。MV3 的 Service Worker 在轮次跑到一半被杀（长任务，或用户关掉侧边栏导致保活
心跳停止）时，`finally` 不会执行，**已经改动的书签就没有任何撤销点**。注意区分：抛异常是有覆盖的
（`finally` 会落盘部分操作），丢的只有"进程被杀"这一种。

现在做两件事：(1) **增量落盘**——事务进行中就把快照写进 `markai.undo.pending`，节奏是
「最多落后 5 条或 250ms，取先到者」+ **尾随补写定时器**（节流只在"下一次记录"时才重新评估，
而 SW 恰恰在**空闲**时被回收，所以必须有定时器把最后几条补上，否则快照正好在最可能被杀的时刻是旧的；
第一版只按时间节流，就是被测试抓出这个缺口的）；(2) **启动时恢复**——SW 启动能看到的 pending
必然来自已经死掉的进程（当前进程还没开始任何轮次），因此可以安全地提升为一个正常撤销点并配一条
"上一轮被中断"的提示，不需要额外的心跳/存活标记。

**(3) 交接：正式点写成功之后才清理快照（2026-09-14 二次修）**——第一版把"恢复"写成
"先删 pending、再写正式点"，于是**正式写入失败时唯一快照已经被删**，改动彻底不可撤销，而恢复函数
仍然返回一个"成功"的点；收尾路径同样忽略了写入结果。现在：正式点**写成功后才清 pending**；
写失败则保留（并补全）pending、返回 null 并留下如实错误；恢复按 `runId` **去重**——
"正式点写成功但清 pending 失败"不会再变成两个都能被回放的点（删除类逆操作重复回放会重复重建子树）。

**如实标注（把此前过强的结论收窄）**：
- 能证明的是：**已成功持久化的快照**在恢复后能精确还原（有测试），且交接失败不再毁掉唯一副本；
- **不能**声称"任意崩溃都不会丢"：快照是节流写入（最多落后 5 条或 250ms），进程若在尾随补写前被杀，
  最后几条改动仍可能不在快照里；`250ms/5 条` 是调度规则，不是持久化完成的保证；
- 消费失败与「清空漏清 pending」已修：消费失败时 `applyUndo` 返回 `ok:false` 并警告不要重复点击，
  同一 SW 会话内拒绝重复应用（跨会话这道保护会消失，是**已标注的残留风险**）；清空会一并清 pending；
- **pending 写入竞态已修**：写入走**串行链**（落盘顺序 = 发起顺序）；收尾/清空**先等在途写入全部
  落定再删键**，因此不存在"清理之后才落地的写入"，连"进程恰好在此刻被杀"的窗口也一并关掉
  （T39 断言 `end()`/`clearUndoPoints()` 返回那一刻在途写入数为 0）。跨进程边界如实标注：
  恢复那侧仍靠 `runId` 去重兜底；
- **栈顶限制已修**：`applyUndo` 只接受当前 `points[0]`，非最新点在任何副作用前零副作用拒绝
  （T42；反证回滚该检查必须变红）。这关闭了「跳选较早撤销点连带删除后续移入数据」的危险路径；
- background 右键指令的 seed 写失败也**已修**（不打开空聊天，改用工具栏提示说清失败）。
- **但仍不能称"撤销这侧已知缺陷全部修完"**（此前的说法已撤回）。仍未处理：
  ①~~**文件夹非空冲突**~~ **已修**：撤销回放前有只读预检，文件夹里有"本轮之外移入"的内容时
  零副作用拒绝并保留撤销点；`applyOne` 的文件夹逆操作改为非递归 `remove` 作纵深防御（T43 九条）；
  ②~~**并发 `applyUndo`**~~ **已修**：在全部只读检查之后、任何副作用之前建立进程内 in-flight
  互斥（检查与登记之间无 await），T46 用可控 create 闸门确定性构造重叠并断言零副作用；
  ③~~**消费失败仍可能显示成功**~~ **已修**：`undoLast` 先判 `!ok`，消费写失败显示 destructive
  「撤销未完成 + 已还原 N 项 + 不要重复点击」；读取失败另以 `undoUnknown` 标出「状态未知」；
  ④~~**`runId` 不是持久终态墓碑**~~ **已修**：`markai.undo` 增加**有界**（最多 50 条）的
  `terminalRunIds`；`takeUndoPoint` 把「移除该点」与「记该 runId 为终态」放在**同一次写入**里
  （原子，不留"点没了但没记账"的窗口）；恢复流程在提升 pending 前先查终态，已终结者**只清 pending、
  不生成新点**；`clearUndoPoints` 连终态一并清（T47 七条）。
  **跨进程边界**：②的互斥是进程内的，SW 回收后消失（已如实标注，未解决）。
- 至此**撤销发布冻结的四项条件全部满足**。剩余待办：进程内互斥的跨会话覆盖、真实浏览器验证。
  其余 best-effort catch 类别也仍未被逐处复核；
- "Chrome 到底多久杀一次 SW、这个窗口有多现实"需要真机才能量化，我没有那个环境。

**撤销历史（把"盲点一次"变成"看得见的依序回退"）**：撤销栈里本来就有 10 个点（摘要/条数/时间齐备），
但界面只暴露了"最新那个"，更早的点既看不见也选不了。现在会话头部有「撤销历史」面板：
每步显示摘要、会还原多少项、多久以前；容量提示也在这里可见（解释"更早的点为何不见了"）。
纯逻辑在 `describeUndoHistory()`（可单测）。

**必须收窄的一条（原先说错了）**：面板**不是**「跳到任意一步之前」。`applyUndo` 只逆转**该点自己的**
操作，不会回退更晚的点；而"撤销新建文件夹"是递归删除，于是「A 新建 F → B 把已有书签 X 移入 F →
跳选撤 A」会连 X 一起删，A 的快照里却没有 X。因此**撤销栈只能从最新点往下撤**：较早的记录标为
不可执行并说明「请先撤销较新的操作」，后端 `applyUndo` 也在任何书签/顺序/存储副作用之前拒绝非最新点
（零副作用；T42 八条断言覆盖）。客户端的禁用只是提示——手工调用、旧客户端、多窗口都能绕过 UI，
所以安全边界必须在后端。

**容量护栏（删除快照带来的新问题，已修）**：删除快照让撤销点可能很大。`chrome.storage.local`
的配额是 **10485760 字节（10 MiB）**（来源：Chromium `extensions/common/api/storage.json` 的
`local.QUOTA_BYTES`；只有申请 `unlimitedStorage` 才被忽略，本扩展没申请），且**超限时写入直接失败**。
原先 `writeUndoPoints` 把这个错误 catch 掉了——于是超配额会表现为「没有可撤销的操作」，
把失败说成了「本来就没有」。现在：撤销点按 **4 MiB** 预算裁剪（余下留给聊天与配置），
**新的优先、丢最旧的**；单点过大则**单独跳过**（不牵连其他点）；两种丢弃都写进存疑说明并在界面提示一次。
落盘失败也不再静默，`readUndoState()` 会把它作为 notice 上报。

**手工确认的删除也已可撤销（原先记为边界，现已闭合）**：`executeDeletions` 从 background 抽到
`src/lib/ai/deletion-executor.ts`（顺带结束它零测试覆盖的历史），删除前做只读预扫描 → 对每个将失去
子项的父目录拍顺序检查点 → 用带快照的 `jRemove` 删除，因此用户在待删清单里确认的清理也能一键还原。

一个必须踩对的细节：**若此刻已有 Agent 轮次的事务在记录，就并入那一轮**（`isRecording()`），
既不开新事务也不结束它——否则会把一个轮次的日志切成两段。收尾放在 `finally`，
避免异常留下活动事务、把后续操作串进上一轮。

顺带说明：给删除加快照**不削弱确认闸门**——闸门决定何时被允许删，撤销只增加一条事后回退路径。

**P2 补上的边界（同样是先失败、后修复）**：规模测试（5270 节点，跑 auto_categorize +
sort_folder + merge_folders 再撤销）抓出**并发批量移动**的坑——`auto_categorize` 用 10 路并发
逐条 `jMove` 埋点，每个 worker 读到的 `fromIndex` 来自**正在被同伴修改**的兄弟列表，
那组下标不构成任何一致的串行历史 → 撤销后节点齐全但**顺序错乱**。

处理方式值得记下来（可复用的排查套路）：**先把并发池改成 1 跑一遍**——
过了就锁定根因是"并发读下标"，而不是撤销算法本身；再动手改。

修复是给日志加一种自包含的批次操作 `moveBatch`：整批一条，并带上**批次开始前源文件夹的完整子序**。
它是批次自身的"操作前状态"，与撤销的逆序回放天然一致（不与其他操作共用下标坐标系），因此抗并发；
同时省掉每条一次 `getChildren`，**比串行更快**（实测一轮 22.3s（串行）→ 8.3s（并发+批次））。
批次在按钮上按**条数**计权重（800 条自动分类不会显示成「1 项」）。

另有两个一致性缺陷一并修掉：
- 撤销按钮展示的是某个具体撤销点，点击却只发「撤销最新的那个」——别的窗口中途完成一轮就会**撤错对象**。
  现在 store 显式取当前展示点的 id 下发；该点已不存在时如实拒绝（"已在另一个窗口撤销过"），
  **不退化成撤销最新点**（那会撤销一个用户没点过的操作，比失败更糟）。
- `chrome.storage.onChanged` 原先只对聊天存储触发刷新，`markai.undo` 变化不通知其他窗口 →
  会留下一个"点了会撤错对象"的陈旧入口。现在撤销点存储变化也触发各窗口刷新。


### B. 可证（Proof）—— 让替身与真实浏览器语义对齐

- 替身升级：`move` 实现 Chromium 的 `index` 语义（含 `index == old_index + 1` 的空操作、
  同父向后移动的 `index--`），并支持 `create` 的 `index`。
- 把散在 React 组件里的重排换算抽成**纯函数**（如 `src/lib/bookmarks/reorder.ts`），
  让两个拖拽面共用同一实现，并在 node 测试里直接覆盖。
- 原则：**任何"我们声称支持"的浏览器行为，替身必须能证伪它。**
  替身实现不了的行为，要么补替身，要么明确标注为"未经证实"。

### C. 可扩（Scale）—— 大库不塌

已有 `check_urls_bulk`（并发 20）、`auto_categorize`（并发 10）、分页导出。
缺口是**没有规模化的验收**：没有 5000+ 节点的测试库，也没有针对 Agent 一次操作上千项的回归。
这条排在 A、B 之后：性能问题会暴露自己，信任问题不会。

## 5. 优先级阶梯

| 级别 | 目标 | 为什么在这个位置 |
| --- | --- | --- |
| **P0** ✅ | 落库现有修复 + 替身升级 + 补齐回归测试 + README 与计数对齐 | 未提交的工作是纯风险；假绿测试必须先拆掉，否则后面所有结论都不可信 |
| **P1** ✅ | 操作日志 + 撤销（主线 A） | 最大产品空洞：只有删除可逆（已完成，见 §4A） |
| **P2** ✅ | 撤销的边界与规模：真实大库（5000+ 节点）下的撤销正确性与耗时、撤销栈的跨窗口一致性、排序/合并/自动分类的专项反证（主线 B/C） | 让 P1 的边界也被证明（已完成，见 §4A 末段：抓出并发批量移动的顺序 bug） |
| **P2+** ✅ | 删除可撤销（快照 + 顺序检查点，见 §4A） | 用户最可能后悔的场景就是自动清理，此前它整轮不可撤 |
| **P2++** ✅ | **手工确认的删除也可撤销**（抽出的 deletion-executor 自开撤销点，见 §4A 末） | 这条此前被写成"边界"：用户自己勾选确认的删除反而是永久的 |
| **P3** ⛔（环境受限，已定性） | 真浏览器端到端验证（加载 `.output/chrome-mv3`，跑一次真实整理任务 + 点一次撤销） | 见下方「P3 为什么做不到」——不是"没时间"，是**本环境没有能带扩展启动的浏览器**；解除条件已写清 |
| **P4** | 分发就绪（权限最小化、隐私说明、上架材料） | 无人值守下无法完成商店审核，只能把可自动化的部分做完 |

**P3 为什么做不到（实测，2026-09-14）**：本环境能用的浏览器自动化是 **ego-browser 0.5.0.32
（Chromium 152）**，它是 **CDP 附着的封装**——skill 明确要求"不要启动另一个浏览器"，
因此无法用 `--load-extension` / `--disable-extensions-except` 启动。三条实测证据：

1. `task.cdp(...)` 只接受 `Target.` 与 `Browser.` 域 → 拒绝 `Extensions.loadUnpacked`；
2. `page.cdp("Extensions.loadUnpacked", …)` → **`Method not available.`**
   （该域要求浏览器以 `--enable-unsafe-extension-debugging` 启动）；
3. Chrome 的"加载已解压扩展"走的是**原生目录选择器**，不是 `<input type=file>`，无法用
   `setInputFiles` 绕过。

**解除条件（谁能做、怎么做）**：在一个能自行启动 Chromium 的环境里，
用 `--disable-extensions-except=<repo>/.output/chrome-mv3 --load-extension=…` 启动，
再跑「自动清理 → 撤销」「两个窗口各点一次撤销」两条路径。
在那之前，README 与发布说明都只声明"经替身测试与产物校验"，不声明真机验证。

> 规模结论的适用边界（别过度解读）：替身的 `getChildren` 是 **O(全库)**，而真实 API 是
> O(该文件夹子项)，所以 5000 节点用例里的耗时（一轮 ~6-11s、撤销 ~6-7s）是**悲观上界**，
> 只能用来抓自家代码的 O(n²) 爆炸与顺序错误，**不代表真实浏览器性能**。
> 真要谈性能，得在真机上量（P3）。

> 版本节奏（已执行）：P0 的修复没有单独发版，与 P1（撤销）一起打成 **v0.2.2**（头部功能才值得一个版本号）；
> P2 修掉的撤销缺陷在 v0.2.2 里**已经发布**，因此单独发了补丁版 **v0.2.3**（含说明"修了什么、是否该升级"的发布说明）。
> **v0.2.4** 收了「撤销覆盖删除」与容量护栏两块（发布说明里写明了升级须知：v0.2.3 及更早写下的旧撤销点没有删除快照，会被如实拒绝整轮撤销）。
> **v0.2.15** 收了「打开链接失败不再被吞掉」（统一走 `open-url.ts`：失败可见、批量按实际结果
> 报数、提示只显示 URL 来源；弹窗补挂 toast 视口并改为打开成功才关），并把错误台账 §3 的
> 三条高风险类别逐项判定完毕。
> **v0.2.14** 收了撤销完整性六连修（栈顶限制 / 文件夹内容预检 + 非递归删除 / 并发互斥 /
> 失败反馈失真 / `runId` 持久终态）与右键指令失败反馈；发布说明如实列出两条未闭合边界
> （跨会话互斥、容量裁剪不记终态）与「仍未做真机验证」。
> **v0.2.13** 收了撤销的四道完整性修复（交接 / 消费失败 / 清空漏清 / 写入竞态）。
> **v0.2.12** 修掉设置保存的静默失败（含删除模式这一安全相关字段）。
> **v0.2.11** 修掉对话存储的同类静默失败（空 catch）+ 6 MiB 对话预算与常驻警示。
> **v0.2.10** 修掉「轮次被中断就丢掉撤销记录」（增量落盘 + 启动恢复）。
> **v0.2.9** 把 dryRun 预览扩到 cleanup_sweep 与 auto_categorize（三个批量工具齐）。
> **v0.2.8** 收了「一键去重」（确定保留规则 + dryRun 预览，工具数 30 → 31）。
> **v0.2.7** 收了「撤销历史面板」（撤销从"盲点一次"变成可跳选的可控回退）。
> **v0.2.6** 收了「手工确认的删除也可撤销」（此前那一步是永久的）。
> **v0.2.5** 收了「隐私/权限文档」「停发未验证的 Firefox 产物」「清空本地数据现在也清撤销记录」。资产列表本身就是停发 Firefox 的证据：本版只有 chrome zip。
> 发版流程与验证方式见 `docs/release.md`（markai 不走 keepgoal 部署主机：扩展没有服务端与探针）。
> v0.2.2 那次发版顺带发现并修掉了 CI 里一个长期存在的静默失败，见 §3 末尾（v0.2.3 的发版已复验该修复在 tag 触发的真实发版里生效）。


### D. 能力（Capability）—— 把易错的模型链路换成确定性工具

**一键去重（`dedupe_bookmarks`）**：原先 `find_duplicates` 只报告分组，清理要靠模型把每组的 id
逐个抄进 `propose_deletions`——几百组时既费 token 又极易抄错。现在把「保留谁」固化成**确定且可解释**
的规则（自定义标题优先 → 最近使用 → 最早收藏 → 按 id 稳定排序，`src/lib/ai/dedupe.ts` 纯逻辑），
工具只在既有安全闸门内执行：`dryRun:true` **只出计划、不落盘**（这是"预览"在最高危操作上的第一次落地），
确认后按 confirm/auto 模式提交提议或直接删除（删除可撤销）。

这条主线已经扩到**三个批量高危工具**：`dedupe_bookmarks`、`cleanup_sweep`、`auto_categorize`
都支持 `dryRun` 先出计划（不落盘、不产生提议、不产生撤销点），预览的计数被测试强制**与随后真实执行一致**
（预览不许说谎），且三者的真实执行路径行为不变（既有用例继续全绿）。

下一步仍是把 dry-run 推到更外层：整体轮次级预览（模型先说完整计划、确认后一次性执行），
那需要工具层与循环层配合，比逐个工具加参数大得多。

## 6. 每个目标的完成标准（Definition of Done）

一个目标只有在**留下可复现的证据**时才算完成：

1. `npm run compile` 通过；
2. `npm test` 通过，且**新增测试能证伪它声称守护的 bug**（先把修复回滚、确认测试变红，
   再恢复——"测试通过"本身不是证据，"测试能失败"才是）。本项目的执行机制是
   `node scripts/falsify.mjs`：逐条回滚实现并要求对应用例变红，任何一条假绿都会非零退出。
   ⚠️ 它会真实改写工作区源码，禁止与 `tsc`/测试/构建并行运行（本项目已因此看到一个假的编译错误）。
3. `npm run build` 通过；
4. README / 文档中的计数与描述与实际一致（不让文档说谎）；
5. 结论写进提交信息，代码落到 `main`（仓库已授权 push）。

绝不允许：把"我检查了，一切正常"当作完成；把叙述当证据；用假替身换来假绿。

## 7. 非目标

- 不做同步服务端 / 账号体系（与"本地优先"定位冲突）。
- 不引入 Hilt/Koin 式的重型框架，不为测试引入 jsdom 全家桶——本项目测试基座是 node + 手写替身，
  轻、快、可读，是资产不是债务。
- 不为了让测试好写而弱化真实语义（例如"反正测不到就忽略 index"——本轮刚因此抓到一个假绿）。
- 不声称与任何商业书签产品对等。

## 8. 无人在场时的决策规则

1. **先读再断**：任何结论前先跑命令、先读文件；涉及浏览器行为先查权威来源（Chromium 源码）。
2. **阻塞就地解决**：换思路、换做法、缩小范围；绝不请求确认、绝不等待批准。
3. **宁可承认"未证实"**：拿不到证据的能力，在 README 与 DIRECTION 里标注，而不是含糊带过。
4. **一次只推进一档**：P0 没落库，不开 P1；防止在半成品上再叠半成品。
